import Placeholder from '../components/Placeholder'

export default function ExportScreen() {
  return (
    <Placeholder
      title="Export"
      description="Generates a Salesforce-ready file and a QuickBooks Invoice file (Property as Customer, Tree as Item, lbs as Quantity) for completed, reviewed harvests. Manual export, run on demand — see design doc Section 3.5."
    />
  )
}
