import Placeholder from '../components/Placeholder'

export default function Admin() {
  return (
    <Placeholder
      title="Admin"
      description="Manage properties, trees, fruit types, and volunteers directly — the field-data equivalents of editing records in Salesforce, without a separate session."
    />
  )
}
