import Placeholder from '../components/Placeholder'

export default function ArchiveHistory() {
  return (
    <Placeholder
      title="Archive / History"
      description="Past harvests and drop-offs, kept out of the active lists once exported. Useful for looking up a property's prior yield without cluttering Today's Stops."
    />
  )
}
